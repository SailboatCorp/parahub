name = "parahub"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-22"

# Cloudflare Pages dashboard bindings required:
# D1 binding name: DB -> parahub-db
# No R2 binding is required for this D1-only build.
# Voice/photo media is stored directly in D1 as compressed/base64 data.
